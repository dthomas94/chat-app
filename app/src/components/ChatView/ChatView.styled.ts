import Box from "@mui/material/Box";
import styled from "styled-components";

export const StyledChatView = styled(Box)`
  display: flex;
  flex-direction: column;
  flex-grow: 1;
  height: 100%;
  overflow: scroll;
`;
